function calculate(myArray) {
    // return 10
    num = eval(myArray[0] + myArray[1] + myArray[2]);
  
  }
  calculate([8, '+', 2])
  console.log("**** This is result from calculate exercise ****")
  console.log(num);