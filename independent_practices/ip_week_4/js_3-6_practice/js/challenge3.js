function calculateSum(number) {
    // sum = number * (number+1);
    // console.log(sum);
    // return sum;
    let sum = 0;
    for (let i = 0; i <= number; i++)
        sum += i
        console.log(sum)
}
    // return sum;
calculateSum(10)
